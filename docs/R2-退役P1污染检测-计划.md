# R2：退役 P1 污染检测 — 清理计划

**更新（2026-08-17）：结论——不做，本计划关闭。** R2 前提「R1 后 `polluted()` 恒 false、P1 是死代码」被实测证伪：探针（/tmp/r2-probe，真实管线验证 20+ 真实文档 + 构造边界）发现唯一触发场景——「开 fence ≤3 空格、闭 fence ≥4 空格」的不对称缩进（detect 的 `isFenceLine` 任意缩进都认，markdown.yaml 的 `^ {0,3}` 只认 0~3 空格，两套判定在闭 fence 方向分歧，region 未关、后续正文行被污染，P1 兜底）。按 §4 自己的策略「有洞应修 R1」，但修根因（统一 fence 缩进判定）已决定放弃：该场景 docs 全量 0 例、现实中基本是误写，罕见程度高于已决定不修的 D11 嵌套 fence 高亮问题。洞仍在，P1 是近零成本的保险丝，删它要拿「−50 行 + 论证重写 + 罕见场景隐性不一致」去换，不划算。若未来重提，前提是先统一 detect 与 markdown.yaml 的 fence 缩进判定。

**前置依赖**：`docs/R1-语法引擎pattern遮蔽修复-fence隔离-计划.md` 已落地并通过全部验收（引擎遮蔽修复 + codeblock 块级 fresh 均生效）。本计划在其后执行。

## 1. 背景与依据

### 1.1 P1 是什么、为什么当时要做

v1.1.26（commit `2c6a26e1`，R0 计划）引入：非 codeblock 段渲染前检测 buffer Match 是否被跨行 region 粘滞污染（`polluted()`：出现非 `md-` 前缀组即判污染），污染则用一次性 Highlighter 做单行 fresh 重高亮（`freshLineMatch()`）。当时引擎缺陷未修、块内颜色无隔离手段，这是唯一能让块外正文恢复配色的下游补丁。

### 1.2 R1 落地后 P1 成为死代码的结构论证

P1 检测的是「非 codeblock 段的行取到被污染的 buffer Match」。R1 之后这个污染源结构性消失：

| 段类型 | 颜色来源（R1 后） | 污染可能性 |
|---|---|---|
| codeblock 段 | 块级 fresh（nil state 起步，R1-B） | 块内残留封在块尾，构造上不跨 fence |
| 非 codeblock 段（正文/标题/列表等） | buffer Match | markdown.yaml 顶层的跨行 region 只有 fence 一种；fence 行必被 P0 detect 判为 codeblock 段——不存在「fence region 粘滞影响到非 codeblock 行」的路径 |

两类污染（引号被 pattern 遮蔽类、真·未闭合字符串类）分别被 R1-A、R1-B 兜住，`polluted()` 永远返回 false，`freshLineMatch()` 永不被调用。

### 1.3 为什么不 git revert v1.1.26

`2c6a26e1` 同时包含 P0（fence 驱动 detect）与 P3（tilde/HR），revert 会把本来正确的部分一起撤掉，detect 退回 state 驱动后真·未闭合字符串场景重新吞段。P1 采取**前向删除**。

## 2. 撤销清单

全部在 `internal/display/`，R1 落地后的实际形态为准（R1 已把展开循环抽成 `expandMatch`，此处按 R1 后形态描述）：

| # | 对象 | 动作 |
|---|---|---|
| 1 | `polluted()`（bufwindow_md.go） | 删除 |
| 2 | `freshLineMatch()`（bufwindow_md.go） | 删除（块级 fresh 用的是 `freshBlockMatches`，不受影响） |
| 3 | `expandLineStyles` 中「非 codeblock 且 polluted 则换 fresh Match」的分支及 `isCodeBlock` 参数 | 删除；函数退化为「取 buffer Match → `expandMatch` 展开」；调用点 `renderSegmentMD` 同步（codeblock 分支在 R1 已不经过此函数） |
| 4 | `TestPolluted`、`TestFreshLineMatch`（bufwindow_md_test.go） | 删除 |
| 5 | 与 polluted 相关的 highlight import、注释 | 随动清理 |

**明确不删**：

- `Segment.IsCodeBlock`（internal/md/md.go）：R1 块级 fresh 的分叉依据，继续使用。
- `freshBlockMatches` / `expandMatch`：R1 的正主，保留。
- P0 的 `isFenceLine` / `findOpenFenceBefore` / `closeOpenStructures`、P3 的 `isClosingFence` / `isHR`：本来就对，保留。
- R0 新增的 4 个 detect 用例（TestDetectCodeBlockWithLoneQuote 等）：结构层回归锁定，保留。

## 3. 验证

1. 编译 + 全量测试：`make build`；`go test ./internal/md/ ./internal/display/ ./pkg/highlight/`——除 R0 遗留的 3 个既有 FAIL（语义决策未定，维持现状）外全绿。
2. T1 文档：与 R1 验收时的渲染**逐像素一致**（本次是删死代码，任何视觉变化都是回归）。
3. 自造真·未闭合字符串样例（sh 块 `it's`）：同 R1 验收第 2 项，不因 P1 移除而劣化。
4. 干净文档（docs/sample.md 等）逐像素不变。
5. `grep -rn "polluted\|freshLineMatch" internal/` 无残留。

## 4. 风险与边界

| 风险 | 评估 |
|---|---|
| 存在 R1 论证未覆盖的污染路径，删 P1 后正文变白 | 论证穷尽了 markdown 顶层跨行 region（只有 fence）；验收第 2/3 项显式覆盖两类污染样例。若真出现，说明 R1 有洞，应修 R1 而非保留 P1 补丁 |
| 编辑模式 / 非 md 文件的 buffer Match 污染 | 与 P1 无关：P1 只在 `renderSegmentMD`（MD 阅读模式）路径生效；编辑模式与非 md 文件用原生渲染，一直是 micro 原版行为（用户已实测确认原版同症），不属本计划范围 |
| 误删 R1 依赖物 | 撤销清单逐项列明保留项；步骤 5 的 grep 兜底 |

## 5. 实施步骤

| # | 改动 | 验证 |
|---|---|---|
| 1 | bufwindow_md.go：删 `polluted` / `freshLineMatch` / `expandLineStyles` 的 isCodeBlock 分支与参数，调用点同步 | 编译过 |
| 2 | bufwindow_md_test.go：删 `TestPolluted` / `TestFreshLineMatch` | `go test ./internal/display/` 绿 |
| 3 | §3 验证全项（T1 逐像素对照 R1 验收基准） | 全过 |
| 4 | grep 确认无残留 | 无输出 |

预计改动量：净删约 −50 行（含测试），无新增逻辑。

完成后按惯例走 changelog / release skill 出版本（标题前缀 refactor 或 chore）。
